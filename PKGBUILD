# TODO: finish up this pkgbuild and make suree everything works along uploading this package to AUR
#
# pkgname=peppy
# pkgver=1.0.0
# pkgrel=1
# pkgdesc="A minimal Python + PyQt6 application launcher"
# arch=('x86_64')
# url="https://github.com/Deepanshusharwan/peppy"
# license=('MIT')
# depends=('python' 'pyqt6')
# makedepends=('git' 'python-pyinstaller' 'python-uv')
# source=("git+https://github.com/Deepanshusharwan/peppy.git")
# md5sums=('SKIP')
# 
# build() {
#   cd "$srcdir/$pkgname"
# 
#   chmod +x linux_build.sh
#   chmod +x build.sh
#   ./build.sh
# }
# 
# package() {
#   cd "$srcdir/$pkgname"
# 
#   # Install binary
#   install -Dm755 dist/peppy "$pkgdir/usr/bin/peppy"
# 
#   # Install icon
#   install -Dm644 peppy.svg "$pkgdir/usr/share/icons/hicolor/scalable/apps/peppy.svg"
# 
#   # Install desktop file (ensure it's in repo or created during build)
#   install -Dm644 peppy.desktop "$pkgdir/usr/share/applications/peppy.desktop"
# 
#   # Install license
#   install -Dm644 LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
# }
# 
